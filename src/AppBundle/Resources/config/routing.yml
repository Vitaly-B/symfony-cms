homepage:
    path:     /
    defaults: { _controller: AppBundle:Default:index }

page:
    path:     /page/{id}
    defaults: { _controller: AppBundle:Page:index }

products_list:
    path:     /products/{page}/{categoryId}
    defaults: { _controller: AppBundle:Catalog:index, page: 1, categoryId: null }
